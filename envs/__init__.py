"""
Project-local environments.

This package exposes the environments shipped in the repository-level `envs/`
folder (e.g. `envs.FlipEnv`). Keeping this as a real package avoids dynamic
import fallbacks and makes imports consistent.
"""

